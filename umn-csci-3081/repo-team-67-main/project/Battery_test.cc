// #include "battery.h"
// #include "drone.h"
// #include "gtest/gtest.h"

// class batteryTest : public :: testing :: Test {
// public:
//   Drone drone;
//   float watts = 1000000000;
// protected:

// };

// TEST_F(batteryTest, myBatteryTest) {
//   EXPECT_EQ(watts, 1000000000) << "Power is not correct";
// }
// //can't get google tests to work on personal Computer, can't get VScode to work on vole,
// //trying to use atom but not sure how to fix in order to run tests after getting many errors
